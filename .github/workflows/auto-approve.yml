name: Auto approve

on:
  pull_request:
    types:
      - opened
      - edited
      - assigned
      
jobs:
  auto-approve:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if: github.actor == 'freeze2222'
    steps:
      - uses: hmarr/auto-approve-action@v4
